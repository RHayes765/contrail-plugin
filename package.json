{
  "name": "contrail-plugin",
  "version": "0.19.0",
  "private": true,
  "description": "Marketplace-install manifest: the two native N-API dependencies the bundled server (server/dist-plugin/index.mjs) resolves at runtime. Prebuilds ship inside these tarballs, so npm ci --ignore-scripts is sufficient — no build step.",
  "dependencies": {
    "@napi-rs/keyring": "^1.1.6",
    "better-sqlite3": "^13.0.3"
  }
}
