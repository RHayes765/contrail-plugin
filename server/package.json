{
  "name": "@contrail/engine",
  "version": "0.7.0",
  "description": "Contrail Engine — Salesforce metadata engine and connection manager, shipped as a local MCP server",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "contrail-engine": "dist/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "node scripts/bundle.mjs",
    "mcpb": "node scripts/mcpb.mjs",
    "test": "vitest run",
    "start": "node dist/index.js",
    "prepare": "node scripts/prepare.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@napi-rs/keyring": "^1.1.6",
    "better-sqlite3": "^13.0.3",
    "fast-xml-parser": "^4.5.0",
    "fflate": "^0.8.2",
    "open": "^10.1.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
